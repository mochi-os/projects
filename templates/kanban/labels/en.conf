[labels]

# Template metadata (shown in the "Create project" picker)
template_name = Kanban
template_description = Simple board for general tasks

# Object classes (id stays English in the DB; only the name is translated)
class.card = Card
class.checklist = Checklist

# Fields (reused across classes — same key, same translation)
field.title = Title
field.description = Description
field.status = Status
field.priority = Priority
field.owner = Owner
field.due = Due
field.items = Items

# Status options
option.status.todo = To do
option.status.progress = In progress
option.status.done = Done

# Priority options
option.priority.high = High
option.priority.medium = Medium
option.priority.low = Low

# Views
view.board = Board
view.list = List
